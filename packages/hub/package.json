{
  "name": "@vsc-codex-bridge/hub",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js hub daemon for VSC Codex Bridge",
  "main": "out/index.js",
  "scripts": {
    "compile": "npm --prefix ../shared run compile && tsc -p .",
    "watch": "tsc -w -p .",
    "start": "node out/index.js",
    "dev": "npm run compile && npm run start",
    "test": "npm run compile && node --test out/test/*.test.js",
    "test:e2e:pwa": "npm run compile && RUN_PWA_BROWSER_E2E=1 node --test out/test/pwaBrowserHappyPath.e2e.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.6.3"
  }
}
